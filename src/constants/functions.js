/**
 * React Native Project
 * File Name: constants.js
 *    Stores all the constant functions
 *
 * Author(s):
 *  1. Kameshwar Nayak
 */

/**
 * Check in the given object is undefined
 *
 * @param {Object} obj - object to be verified
 */
export const isUndef = (obj) => {
  return obj === undefined || obj === null || obj === '' || obj === 'None';
};
